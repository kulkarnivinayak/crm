language: php

php:
  - 7.0

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  - php artisan key:generate
  - npm install
  - npm install gulp
  - gulp

script:
  - vendor/bin/phpunit